//* DECLARAMOS FUNCIONES

function esDeclarada() {
  return null;
}

const funcExpresada = function () {};

// Funcion flecha o arrow function

const funcFlechaSumar = (a, b) => a + b;

// sin {} en una linea y return está implícito
const funcFlechaSumarEnMasLineas = (a, b) => {
  return a + b;
};
